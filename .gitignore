# One-off recovery/maintenance scripts — not part of the app
delete-aretha.html
clean-pins.html

# Segredos — nunca commitar
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
credentials.json
secrets.json
service-account*.json
service-account-key.json
*-firebase-adminsdk-*.json
roles.json

# Config local do Claude Code
.claude/

# Dependências e backups
node_modules/
# package.json/lock existem só por causa do set-claims.mjs (firebase-admin via npm).
# O app é HTML estático servido pelo GitHub Pages — o repo NÃO usa package manager.
package.json
package-lock.json

# Dados locais sensíveis (não devem ir pro repo)
*.xlsx
*.xls
*.json.bak
backups/
mahau-crm-backup-*.json
